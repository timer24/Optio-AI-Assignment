# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.angular/
.next/
.turbo/
.cache/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# TypeScript
*.tsbuildinfo

# Coverage
coverage/

# Claude Code local-only settings (per-user; shared settings would be in .claude/settings.json)
.claude/settings.local.json

# Prisma — generated client lives inside node_modules so we don't need to ignore it explicitly,
# but if we ever output to a custom path we'd add it here.
