// Shared types between API and Web.
// Populated as we build out the domain — segments, deltas, events, DTOs.
// For now this file exists so the workspace package resolves; we'll fill it
// in on Day 2 when we design the segment evaluation API.

export {};
