{
  "name": "@drift/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types between API and Web (DTOs, event payloads, enums).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
