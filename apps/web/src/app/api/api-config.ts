// Single source of truth for the API base URL. Hardcoded for the dev
// environment — a production build would inject this via Angular's
// environment files or a runtime config endpoint.
export const API_BASE = 'http://localhost:3000';
